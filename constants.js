const NEXT_PLAYER = {
    'N': 'E',
    'E': 'S',
    'S': 'W',
    'W': 'N'
};

const SUITS = ['S', 'H', 'D', 'C'];

const SUIT_RANKS = {'S': 0, 'H': 1, 'D': 2, 'C': 3};

module.exports = {
    NEXT_PLAYER,
    SUITS,
    SUIT_RANKS
}
